import React from 'react';
import {Icon, IconsPropsType} from "./Icon";

export const LinkedInIcon = (props:IconsPropsType) => {
    return (
        <Icon iconId={'linkedInIcon'} width={'33'} height={'33'} viewBox={'0 0 33 33'} />
    );
};

