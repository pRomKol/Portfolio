import React from 'react';
import styled from "styled-components";

export const Button = () => {
    return (
        <StyledButton>
            Let`s Begin
        </StyledButton>
    );
};
const StyledButton = styled.button``

